# V2-12 — DevOps, Production Readiness, and Deployment Governance

Status: `IN PROGRESS — V2-12.1`

## Authority and objective

This canonical item was established under explicit project-owner authorization
because the repository had no prior `docs/roadmap` structure. It is based on
the V2-11 closure evidence, `docs/project3/` controls, current repository
configuration, and the owner-approved V2-12 definition.

Primary objective: prepare the SIST Student Services and Administration Portal
for controlled Preview and Production operation through documented environment
separation, CI quality gates, secure secret management, deployment governance,
database operations, observability, recovery, rollback, and final operational
handoff.

- Immutable base: `34994d8a6db151bd5f2191a392c757c5ef7feade`.
- Branch: `codex/v2-12-devops-production-readiness`.
- Worktree: `D:\PROJECT 3\PROJECT 3 SIST-v2-12`.
- V2-11 automated Chromium evidence: 49 passed, 0 failed.

Documentation alone does not close V2-12. Planning, implementation, external
configuration, verification, rehearsal, and project-owner Production approval
are separate gates.

## Canonical subphases

| Subphase | Status | Approved scope |
| --- | --- | --- |
| V2-12.1 — Roadmap and Production-Readiness Governance | `IN PROGRESS` | Roadmap, boundaries, governance, inventory, dependencies, and decisions. |
| V2-12.2 — CI Quality Gates and Release Controls | `NOT STARTED` | Implement CI using existing scripts and define release-protection evidence. |
| V2-12.3 — Preview Environment Readiness | `NOT STARTED` | Isolated Preview configuration, migration, and workflow verification. |
| V2-12.4 — Production Database, Secrets, and Deployment Runbooks | `NOT STARTED` | Production inventory, controlled migration, backup, deployment, and rollback runbooks. |
| V2-12.5 — Storage, Email, Observability, and Incident Readiness | `NOT STARTED` | Provider decisions/configuration, redacted logs, alerting, and incident playbooks. |
| V2-12.6 — Final Hardening, Deployment Rehearsal, and Handoff | `NOT STARTED` | Manual hardening, rehearsals, readiness review, and administration handoff. |

## Prerequisites and dependencies

- V2-11 closure is the immutable base and worktree/branch isolation is mandatory.
- No Production operation may occur without explicit owner authorization.
- Preview and Production must use separate databases and separate secret scopes.
- Production student data must never be used for destructive testing.
- Authorized owners must supply required external credentials and institutional decisions before the corresponding implementation pack.

Repository evidence identifies PostgreSQL/Neon through Prisma, Vercel Blob storage abstraction, Resend email abstraction, Auth.js authentication, and existing npm verification scripts. There is no checked-in `.github` workflow, `vercel.json`, monitoring integration, backup/recovery runbook, or incident playbook at this base commit.

## V2-12.1 governance plan

### Environment model

| Environment | Boundary and permitted use |
| --- | --- |
| Local | Developer-owned; local or isolated test database only; local-only credentials, safe substitutes, and no Production data. |
| Preview | Vercel Preview or approved equivalent; isolated Preview database and secrets; sanitized/test data only; release-candidate verification. |
| Production | Human-approved release only; Production-only database and secrets; no destructive testing; restricted operational ownership, recorded deployment, and rollback evidence. |

Promotion is Local → Preview → an approved Production release commit. Preview and Production must never share a database or secret scope.

### Secret governance

The current example/environment code identifies `APP_URL`, `DATABASE_URL`, `DIRECT_URL`, `TEST_DATABASE_URL`, `AUTH_SECRET`, document-storage settings, and development seed controls. Storage code also requires a Vercel Blob token or store identity; email code requires a Resend API key and sender address. These names/categories are documentation only, never values.

- Secrets are scoped separately for Local, Preview, and Production and remain outside Git, logs, reports, screenshots, and test artifacts.
- Startup/build validation must fail safely with field-only configuration errors; values must never be reported.
- A designated secret custodian and rotation owner must maintain inventory, access control, routine rotation, and emergency revocation/replacement.
- Emergency rotation requires containment, replacement in the affected scope, application/release verification, access review, and sanitized incident evidence.

### Git and release governance

- Production may be released only from an approved, reviewed commit with a clean synchronized branch and recorded verification evidence.
- Human Production approval is mandatory. Force pushes and history rewrites are prohibited for release history.
- Release evidence records the commit, reviewer/approver, checks, environment, migration decision, deployment result, and rollback reference.
- Rollback uses an identified previously approved release commit or provider release reference; a forward fix is preferred when database state makes code rollback unsafe.

### CI gate plan

Future V2-12.2 CI must use existing scripts rather than invented commands:

| Gate | Existing repository command |
| --- | --- |
| Locked dependency installation | `npm ci` (lockfile-based installation; no package script currently names it) |
| Formatting | `npm run format:check` |
| Lint | `npm run lint` |
| Type checking | `npm run typecheck` |
| Prisma validation | `npm run db:validate` |
| Unit/component tests | `npm run test` |
| Production build | `npm run build` |
| Browser tests | `npm run test:e2e` where the approved environment supports it |

No checked-in secret-scanning or dependency-audit npm script was found. Those are V2-12.2 gaps requiring a bounded, reviewed implementation decision.

### Vercel plan

Preview deployments must use Preview-only variables, an isolated Preview database, and a verified approved commit. Production deployment requires the same separation, explicit human approval, recorded release evidence, and a known rollback reference. No Vercel project, connection, or deployment is created by V2-12.1.

### Database operations plan

Local, Preview, and Production databases remain separate. Checked-in reviewed Prisma migrations are the only production schema path; `prisma db push`, reset, and destructive production testing are prohibited. A future release runbook must assign migration ownership, require review and approval, take/verify a pre-deployment backup, rehearse restoration in an isolated environment, verify after migration, and record the rollback-versus-forward-fix decision.

### Storage and email plan

Document artifacts currently use a private Vercel Blob abstraction, bounded to 5 MiB and served through authenticated download paths. Test execution can use an in-memory storage driver only under its explicit test-only guard. Production storage provider/account, credential ownership, retention, and recovery remain unresolved.

Notifications use a provider-neutral interface with a Resend implementation, a fake test provider, transactionally claimed delivery rows, idempotency keys, five attempts, exponential retry, and terminal failure state. Production email provider/account, sender domain, credential custodian, and operational delivery worker/schedule remain unresolved. Private attachments and personal data must not be exposed through provider URLs, logs, or notification content.

### Observability, incident, backup, recovery, and rollback plan

No structured application logging, external error reporting, metric, alerting, backup/recovery, or incident documentation was found at this base. Later packs must provide redacted structured logs, correlation identifiers, error reporting, availability and job metrics, actionable alerts, named operational ownership, severity/escalation procedures, backup/recovery evidence, and an isolated rollback rehearsal. Logs and reports must exclude student data and secrets.

## Closure gates

V2-12 may close only when evidence proves all of the following:

1. Required CI checks pass on the approved release commit.
2. Preview and Production boundaries are documented and implemented.
3. Preview uses isolated database and secrets.
4. Production secrets are managed outside Git.
5. Migration, backup, restoration, and rollback procedures are documented and rehearsed in an isolated environment.
6. Preview workflows are verified.
7. Storage and email are safely configured or explicitly disabled with approved operational handling.
8. Logs and error reporting redact private data and secrets.
9. Monitoring, incident ownership, and escalation procedures are defined.
10. Manual Final Hardening is complete or formally accepted as a limitation.
11. Production deployment has explicit human approval.
12. Final release, rollback, synchronization, and handoff evidence is recorded.
13. No unresolved critical security or data-protection blocker remains.
14. Project-owner closure approval is recorded.

## OWNER DECISION REQUIRED

- Production domain and production launch date.
- Vercel account/project owner and deployment approver.
- Secret custodian and rotation owner.
- Production and Preview database owners.
- Production email provider and sender-domain owner.
- Production storage provider/account owner and retention policy.
- Monitoring/error-reporting provider and operational owner.
- Backup retention, RTO, RPO, SLA, data-retention rules, and incident contacts.

## Evidence ledger and Pack 1A boundary

Completed in V2-12.1:

- Canonical roadmap and V2-11 historical record established under owner authorization.
- V2-12 title, sequence, prerequisites, dependencies, closure gates, and governance planning defined.

Not implemented:

- CI workflow or release protection.
- Vercel connection, Preview deployment, or Production deployment.
- Remote migration, backup, or restore execution.
- Secret creation, storage/email provider configuration, or monitoring setup.
- Manual Final Hardening or any V2-12.2+ work.

No deployment occurred, no Production database was accessed, no migration ran, no secret was created or exposed, and no real email was sent.
