# V2-12.4 — Production Database, Secrets, and Deployment Runbooks

Status: `CLOSED — READINESS COMPLETE; LIVE RELEASE DEFERRED`

## Scope and closure

V2-12.4 establishes Production database ownership, secret governance, migration
and recovery procedures, deployment controls, and the operational runbook. It
does not authorize a live Production release.

The repository provides `npm run production:verify`, `npm run db:status`, and
`npm run db:deploy`; the latter is a release-only mutation and was not run.
The focused verifier test passed 1 file / 2 tests. Formatting, type checking,
Prisma validation, secret-free tests, production build, and the scoped security
diff review passed. The existing repository lint failure in the unchanged
`src/components/staff/theme-toggle.tsx` remains outside this package.

## Owner-reported external readiness evidence

- Neon project/database: `student-admin-portal` / `neondb`.
- `DATABASE_URL` is a pooled Production connection for `sist_runtime`.
- `DIRECT_URL` is a direct Production migration connection for `neondb_owner`.
- `sist_runtime` has no superuser, database, role, replication, or RLS-bypass
  privilege; required DML access was verified on 19 public tables and future
  default privileges were configured.
- A distinct Production `AUTH_SECRET` and
  `https://student-admin-portal-phi.vercel.app` application URL are configured.
- Production has no `TEST_DATABASE_URL`.
- Neon provides six hours of PITR plus manual snapshot/restore; the project
  owner / Neon administrator owns restoration.
- GitHub `main` has active Production protection, Vercel Production tracks
  `main`, and the repository/project owner is the release approver.

No secret value is recorded. No Production database, provider API, migration,
backup/restore, or live deployment was performed by this phase.

## Deferred release gate

The project owner explicitly deferred the first real Production deployment until
Final Release/Handoff, after remaining development, polish, bug fixing, final
verification, and handoff. The eventual release must follow
[`../../operations/production-readiness.md`](../../operations/production-readiness.md).

## Result

Production infrastructure readiness: `PASS`.

Actual Production release: `NOT PERFORMED — OWNER DEFERRED`.

Next subphase: V2-12.5.
